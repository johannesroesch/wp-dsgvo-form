/**
 * Save component for the DSGVO Form block.
 *
 * Returns null because this is a dynamic block —
 * rendering is handled server-side by FormBlock.php.
 *
 * @return {null} Dynamic block, no save markup.
 */
export default function Save() {
	return null;
}
